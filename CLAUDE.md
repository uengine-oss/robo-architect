<!-- SPECKIT START -->
Active feature plan: [specs/022-spec-generation-from-event-storming/plan.md](specs/022-spec-generation-from-event-storming/plan.md)

Read the plan for technologies, project structure, constitution gates, and architectural constraints relevant to current work. Companion artifacts in the same directory:
- spec.md (what & why — 이벤트 스토밍 그래프를 "DDD for SDD" 아티팩트 세트로 자동 생성: `domain-terms.md`·`bc-<slug>.md`·`context-map.md`·`aggregate-<slug>.md`·`acl-<system>.md`·`requirements.md`. SpecKit 비연동, Figma 토큰 불필요 — 와이어프레임은 Neo4j `UI.sceneGraph`에서 렌더. 출력: `specs/bounded-contexts/<bc>/` + `specs/context-map.md`. 그래프는 SoT. **2026-05-12**: P5–P7 추가 — 프런트엔드 framework 선언 + `specs/frontend/{framework,menu-structure,ui-flow}.md` (BC 형제 폴더), PRD.md↔CLAUDE.md 콘텐츠 분리, per-BC agent 폐기 후 role-based agent(`frontend-engineer`/`ddd-specialist`) + 새 `/generate-frontend` 슬래시 커맨드. **2026-05-13**: P5 viewport-intent check 추가 — 와이어프레임 primary-frame width로 mobile(≤480)/tablet(481–1024)/desktop(>1024) 분류, `framework.md`에 `## Viewport summary` + `Dominant:` 라인(70% 임계값) 렌더, agent + `/generate-frontend` body에 IA 생성 전 "Viewport intent check" 단계가 mobile/tablet/desktop-first 확인 질문(FR-025/026/SC-014; `frontend_viewport_dominant`·`frontend_viewport_mixed` warning))
- research.md (Phase 0: D1–D6 (v1) + D7–D10 (2026-05-12) + **D11 (2026-05-13)** — viewport classification width-based 70% dominant, code 상수)
- data-model.md (Pydantic 전용 — v1: `BoundedContextProjection`/`AggregateProjection`/`UserStoryProjection`/`WireframeProjection`/`CrossBcFlow`. §6 frontend 추가: `FrontendCompositionProjection`/`FrameworkConventions`/`MenuEntry`/`UIFlowEntry`/`TriggerOrigin`, `ArtifactKind`+`SkippedItem.reason`+`GenerationWarning.code` 확장, `specs/frontend/*.md` 파일출력. **2026-05-13**: `WireframeProjection`/`UIFlowEntry`/`MenuEntry`에 `viewport_class`, `FrontendCompositionProjection`에 `viewport_summary`+`dominant_viewport` 추가. Neo4j 스키마 변경 없음)
- contracts/rest-api.md (`POST /api/ddd-spec/{generate-bounded-context,generate-aggregate,generate-context-map}` (sync) / `/generate-all` (SSE) 변경 없음. §7 신규: `/api/prd/{generate,download}` 요청·응답에 frontend 패키징 정합성 추가 — `frontend_framework_required` 400 사전조건, `specs/frontend/*.md` zip 포함, role-based agent + `/generate-frontend` 커맨드 emit, per-BC agent 미발행, PRD↔CLAUDE lint(`prd_split_lint_failed` 500)는 zip 빌드 시점에 강제. **2026-05-13**: viewport 분류·agent prompt 동작 + `frontend_viewport_dominant`/`frontend_viewport_mixed` warning 추가)
- quickstart.md (manual smoke: 12 scenarios — S1–S7 (v1) + S8 framework 선언 사전조건, S9 풀 패키지 + 인과 UI flow + PRD↔CLAUDE disjoint + per-BC agent 부재 + 모든 ui-flow 링크 도달가능, S10 degraded paths, **S11–S12 (2026-05-13)** viewport intent check — dominant mobile / mixed 두 경로)
<!-- SPECKIT END -->
