<!-- SPECKIT START -->
Active feature plan: [specs/029-robo-spec-skills/plan.md](specs/029-robo-spec-skills/plan.md)

Read the plan for technologies, project structure, constitution gates, and architectural constraints relevant to current work. Companion artifacts in the same directory:
- spec.md (what & why — Claude Code 용 robo-* skill set 4종 (`/robo-plan`, `/robo-tasks`, `/robo-implement`, `/robo-sync`) + MCP 브리지. US1 `/robo-plan <feature-id|BC|Aggregate>` → BC 분류(core/supporting)에 따라 clean vs default-speckit 아키텍처로 `plan.md`만 생성(spec.md/data-model.md/contracts/ 미생성). US2 `/robo-tasks` 체크박스 ↔ Design 탭 노드 진행 뱃지 양방향. US3 Design 탭 노드 클릭 → 워크스페이스 에디터에서 구현 파일 열기. US4 `/robo-implement` plan 레이아웃 강제 + 체크박스 자동 갱신. US5 `/robo-sync` 소스코드 → Robo Architect 역반영(propose→apply, 파괴적 변경은 확인). US6 robo-* skill 은 `/robo-spec/` 루트의 원본을 **Jinja 없이 바이트-동일 복사**로 설치)
- research.md (R1 MCP in-process streamable-HTTP at `/mcp`, R2 `robo-spec/.claude/` shutil.copytree no Jinja, R3 `claude-code-ide-workspace`(spec 021) 워크스페이스 경로 재사용 + `<workspace>/.claude/robo-project.json`, R4 `BoundedContext.classification ∈ {core,supporting}` 신규 선택 속성 + 누락 시 `/robo-plan` 가 묻고 `set_bc_classification` 으로 역기록, **R5 소스 매핑은 그래프 단일 보관 — 신규 `:ImplementationFile` 노드 + `[:IMPLEMENTED_IN]` 관계 (워크스페이스 로컬 매니페스트 없음)**, R6 backend `watchfiles` 가 `tasks.md` 감시 → SSE 로 Design 탭 푸시(5s 이내), **R7 `/robo-sync` 는 풀 AST 익스트랙션 — Python(stdlib `ast`)+TS(`@typescript-eslint/typescript-estree` 노드 헬퍼) 익스트랙터를 workspace 에 verbatim 복사 후 Bash 로 실행, 코드젠 시점 마커 없음, LLM 은 rename 후보 랭킹에만 사용(최종 결정은 개발자)**, **R8 drift 는 무상태 — 로컬 fingerprint 없이 호출 시점에 `nameSeen` vs 그래프 현재 이름 비교**, R9 신규 라우트는 `/api/robo-spec/*` 와 `/api/contexts/{bc_id}/classification` 만, Swagger 필수, R10 `frontend/src/features/robo_spec/` 미러, **R11 robo-* 는 speckit-* 을 SKILL.md 레벨에서 thin delegation+override 로 상속 — Jinja/빌드머지 없음, frontmatter 에 `requires-speckit` 버전핀 + 본문 첫줄에 위임 헤더 + 번호 매긴 override 리스트(단계번호+verbatim phrase 듀얼앵커), `/robo-sync` 는 상속 대상 없음(self-contained)**)
- data-model.md (§1 Neo4j 델타 — `BoundedContext.classification` 선택 문자열(core|supporting), **신규 `:ImplementationFile { projectId, path, role, createdAt, lastSeenAt }` 노드 + `(:Aggregate|Command|Event|ReadModel)-[:IMPLEMENTED_IN]->(:ImplementationFile)` N:M 관계** — 03_node_types.cypher + 04_relationships.cypher 문서화 의무. §2 워크스페이스 온디스크 아티팩트 — `.claude/robo-project.json`, `plan.md`, `tasks.md`(체크박스 마커는 `tasks.md` 내부에만, **소스코드에는 마커 없음**), **§2.4 `.robo-link.json` 부재(의도적 — 소스 매핑은 그래프에만)**. §3 MCP 인프로세스 상태 — `ProjectSession`(pendingProposals 10분 TTL)·`WatchedTasksFile`. §4 교차 검증 — projectId 일관성·element id 해석·소스 매핑 무결성·원자적 쓰기)
- contracts/mcp-tools.md (스트리머블-HTTP `/mcp` 9 tools — T1 `resolve_design_element`, **T2 `get_bc_design` 가 `implementationFiles[]` 까지 묶어 반환(별도 lookup 툴 없음)**, T3 `set_bc_classification`, **T4 `compute_drift` 무상태 — `references[{id,nameSeen}]` 입력 → renamed/deleted/added/reclassified 회신(blocking 3종)**, T5 `report_progress`, **T6 `propose_sync` AST 익스트랙트 입력 + `renameCandidates`(LLM 랭킹)**, T6a `apply_proposal`(version-based CONFLICT), **T6b `register_implementation_files`(replace|merge — `[:IMPLEMENTED_IN]` 작성)**, T7 `open_file_in_workspace`(그래프에서 해석, opened/not-implemented/ambiguous/offline), T8 `subscribe_progress` SSE)
- contracts/http-api.md (신규/확장 라우트 — E1 `POST /api/claude-code/setup-project` 확장(verbatim copy + roboSpecChecksum, projectId 변경 시 409), E2/E3 `GET|PATCH /api/contexts/{bc_id}/classification`(enum 검증·SmartLogger 이벤트), E4 `POST /api/robo-spec/projects/{project_id}/open-file`(**그래프 단독 해석**, opened/not-implemented/ambiguous, 503=offline), E5 `GET .../progress/stream` SSE(재연결 시 full-state replay), **E6 `GET .../implementation-map?bcId=` — 그래프의 `[:IMPLEMENTED_IN]` 프로젝션(워크스페이스 매니페스트 미사용)**. `/docs` 등록 + `robo-spec` 태그 + 모든 응답 Pydantic 모델)
- quickstart.md (manual smoke S1..S14 — S1 verbatim 설치 + Jinja 마커 0, S2 core→clean architecture + **`.robo-link.json` 미생성 검증**, S3 supporting→default, S4 분류 누락→질문 후 역기록, S5 체크박스→Design 뱃지 5s, S6 클릭→파일 열림 2s(그래프 해석), S7 `files: []`→"not implemented yet", **S8 `/robo-implement` 체크박스 + `:ImplementationFile` 등록 + `grep '@robo' src/` 결과 0**, **S9 마커 없이 추가 → AST 추출 후 propose, S10 rename → renameCandidates + 확인 요구**, S11 변경 없음 no-op, **S12 무상태 drift 감지(로컬 fingerprint 없음)**, S13 워크스페이스 오프라인 → "code link offline", **S14 speckit 업그레이드 후 상속 회귀 — `requires-speckit` 범위 안이면 동일 산출물, 범위 밖이면 명시적 경고+컨펌**)
<!-- SPECKIT END -->
