<!-- SPECKIT START -->
Active feature plan: [specs/023-electron-desktop-app/plan.md](specs/023-electron-desktop-app/plan.md)

Read the plan for technologies, project structure, constitution gates, and architectural constraints relevant to current work. Companion artifacts in the same directory:
- spec.md (what & why — Wrap the existing product (FastAPI `api/`, Vue 3 SPA `frontend/`, Neo4j) in an Electron shell so non-developers install a single signed installer for Windows + macOS with zero infra setup. The shell owns the lifecycle of two managed children — bundled CPython/uvicorn + bundled Neo4j — picks free ports, loads the SPA at `http://127.0.0.1:<backend>`, and adds single-instance lock, signed auto-update, Settings (data source + LLM keys), crash recovery, "Reveal logs", per-user data dir. Web/server deploy stays first-class. No new HTTP endpoints, no Neo4j schema change.)
- research.md (D1 Electron 31 + electron-builder + electron-updater + strict CSP/contextIsolation/sandbox, D2 **GPLv3 fork** — bundle Neo4j Community + JRE in installer OR download-on-first-run (resolved in T050), D3 python-build-standalone + `uv` for backend bundling, D4 renderer→local backend via `http://127.0.0.1:<port>` with SSE/WS over the existing permissive CORS, D5 readiness probe = `GET /health` w/ TCP fallback + crash detection + restart policy, D6 `safeStorage` primary + `keytar` fallback for secrets — env injected by main, D7 free-port via ephemeral bind+release + single-instance lock, D8 signed auto-update incl. Authenticode + Apple notarize + interrupted-update safety, D9 writable data-dir resolution + bundled-Neo4j cross-version migration)
- data-model.md (**No Neo4j schema change, no new Pydantic models.** Electron-side only. §Persisted `DesktopSettings` (schemaVersion, dataSource bundled|external, externalNeo4j {uri,user,database}|null, dataDir|null, llm {provider,model}, update {autoCheck,lastCheckedAt}, window {bounds,maximized}, lastPorts) — atomic write, corrupt-file→.bak. §Transient `RuntimeState` (appVersion, backendPort, boltPort, pids, status enum, dataSource, dataDir, updateState). §`SecretRef` enum — neo4j.password / llm.{openai,anthropic,google}.apiKey / figma.apiToken — values only in OS secure store, never in settings.json/logs/git.)
- contracts/ipc-contract.md (Single new external surface. `window.desktop` via contextBridge — **no** raw ipcRenderer/Node leak (FR-021). Queries: `app:getRuntimeState`, `settings:get`, `dataDir:get`. Commands: `settings:set`, `settings:setSecret`, `settings:testNeo4jConnection`, `dataDir:choose`, `backend:retry`, `logs:reveal`, `update:check`, `update:apply`, `app:openExternal`. Subscriptions: `app:onBackendStatus`, `app:onUpdateState`, `app:onDataSourceChanged`. Every `invoke` returns `{ok:true,data}|{ok:false,error:{code,message}}` with a closed `IpcErrorCode` enum.)
- contracts/packaging-manifest.md (Build inputs/outputs, signing requirements — Windows Authenticode + macOS Developer ID + notarization, update-feed contract for electron-updater.)
- quickstart.md (12 manual smoke scenarios across Windows + macOS — install+launch<60s, core workflow, clean shutdown 0 orphan procs, relaunch persists, external Neo4j swap, LLM key swap, update N→N+1 with data preserved, interrupted update, port-conflict auto-recovery, backend-crash recoverable banner, signed installer no "unidentified developer" prompt, 015/021/016 non-regression.)

**Phase progress (this branch, started 2026-05-27):**
- Phase 1 Setup (T001–T006) ✅ — `desktop/` module scaffold, `package.json`, `tsconfig.{base,main,preload}.json`, `electron-builder.yml`, IPC contract types, ESLint/Prettier.
- Phase 2 Foundational (T007–T012) ✅ — lifecycle skeleton, JSONL logger, data-dir basic, IPC envelope plumbing, preload contextBridge, smoke-test skeleton.
- Phase 3+ (US1/US2/US3, T013–T047) ⏸️ paused: pending GPLv3 decision (T050), code-signing certs, network downloads (python-build-standalone, Neo4j+JRE), and validation on real Windows + macOS hosts.
<!-- SPECKIT END -->
