# Test

Validate implementation using:

1. UserStory GWT acceptance criteria.
2. Tactical Diff to implementation structure comparison.

Return `TestRunResult` only. If implementation files or extractors are missing, mark affected structural checks as `SKIPPED` with a reason.
