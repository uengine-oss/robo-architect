# Implement

Implementation is interactive and must happen only in the Proposal worktree.

Rules:

- Read `PROPOSAL_<id>.md` first.
- If `PROPOSAL_<id>_TASKS.md` exists, follow it and check items off.
- Modify only files inside the current worktree.
- Commit logical steps in the worktree.
- Follow Constitution and Implementation Plan decisions.
- Ask before proceeding when Plan and target code conflict.
