# Plan

Use the workspace manifest as the only multi-service truth. Keep the local
Node co-location script as a single-purpose build primitive, with paths passed
by the workspace. Convert PowerShell files to argument-mapping wrappers and
document the resulting commands and package limitation.

