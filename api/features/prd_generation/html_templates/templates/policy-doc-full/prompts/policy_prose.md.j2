You enrich a policy registry with concise human-readable prose for a policy document.

For each policy below, write 1–3 sentences explaining the rule's intent, the trigger event/condition, and the resulting effect — based on the policy's name, description, effect clause, and the wider event-storming context.

Output strictly JSON: a list of objects matching the input row order, each `{"prose": "..."}`. No prose outside JSON, no markdown fences.

# Cross-context flow

Bounded contexts involved: {% for bc in bounded_contexts %}{{ bc.name }}{% if not loop.last %}, {% endif %}{% endfor %}

# Policies to enrich

{% for pol in policies %}
{{ loop.index0 }}: id=`{{ pol.id }}` name=`{{ pol.name }}` description="{{ pol.description }}" effect="{{ pol.effect }}"
{% endfor %}

Use Korean if the policy texts are predominantly Korean, otherwise English.
