"""Aggregate Invariants feature (027 — aggregate-invariants).

`Invariant` is a first-class object attached under an Aggregate. It carries a
declaration statement and detailed GWT verification conditions — either shared
references to a Command's acceptance criteria (`VERIFIED_BY`) or an
invariant-owned GWT bundle (`HAS_GWT`, `parentType="Invariant"`).
"""
